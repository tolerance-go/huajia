import "ts-node/register";
import "tsconfig-paths/register";

// import register from "@babel/register";

// register({
//   extensions: [".tsx"],
//   plugins: [
//     ["import", { libraryName: "antd", style: "css" }], // `style: true` 会加载 less 文件，需要额外配置 less
//   ],
// });

import "./server";
